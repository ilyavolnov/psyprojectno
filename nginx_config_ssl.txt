server {
    listen 80;
    server_name new.dr-rumyantceva.ru www.new.dr-rumyantceva.ru;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name new.dr-rumyantceva.ru www.new.dr-rumyantceva.ru;
    
    root /var/www/psyproject;
    index index.html index.htm;
    
    # SSL certificate
    ssl_certificate /etc/letsencrypt/live/new.dr-rumyantceva.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/new.dr-rumyantceva.ru/privkey.pem;
    
    # SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    
    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API proxy
    location /api/ {
        proxy_pass http://127.0.0.1:3001/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
    
    # Logging
    access_log /var/log/nginx/new.dr-rumyantceva.ru.access.log;
    error_log /var/log/nginx/new.dr-rumyantceva.ru.error.log;
}