# 🎬 Быстрый деплой ДЕМО на Vercel

## Для демонстрации клиенту (без БД)

### Вариант 1: Только Frontend (30 секунд)

1. Переименуйте vercel.json:
```bash
mv vercel.json vercel-full.json
mv vercel-demo.json vercel.json
```

2. Задеплойте:
```bash
vercel --prod
```

**Результат:** Клиент увидит весь дизайн и интерфейс, но без работающего backend.

---

### Вариант 2: С Mock данными (рекомендую)

1. Обновите vercel.json:
```json
{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/demo"
    }
  ],
  "redirects": [
    {
      "source": "/admin-panel",
      "destination": "/admin-panel.html"
    },
    {
      "source": "/admin-login",
      "destination": "/admin-login.html"
    }
  ]
}
```

2. Задеплойте:
```bash
vercel --prod
```

**Результат:** Полностью рабочий сайт с тестовыми данными. Клиент может:
- Просматривать специалистов
- Видеть курсы и супервизии
- Открывать попапы заказа
- Применять промокоды
- Видеть админ-панель с данными

---

## 🎯 Что покажет клиенту:

✅ Полный дизайн сайта
✅ Все страницы работают
✅ Попапы и формы открываются
✅ Промокоды применяются
✅ Админ-панель с интерфейсом
✅ Все анимации и эффекты

❌ Данные не сохраняются (это демо)
❌ Telegram бот не работает
❌ Загрузка файлов не работает

---

## 📱 После демонстрации

Для продакшена нужно:
1. Подключить внешнюю БД (Supabase/MongoDB Atlas)
2. Настроить Telegram webhook
3. Настроить хранилище файлов (Vercel Blob/S3)

Подробнее в `VERCEL_DEPLOY.md`

---

## 🚀 Команды

**Деплой демо:**
```bash
vercel --prod
```

**Посмотреть URL:**
```bash
vercel ls
```

**Удалить деплой:**
```bash
vercel rm [deployment-url]
```

---

## 💡 Совет

Для демонстрации клиенту mock данные (Вариант 2) - идеальный выбор. 
Сайт будет полностью интерактивным и произведет отличное впечатление!
