# Руководство по исправлению фотографий специалистов

## 🎯 Проблема

Некоторые изображения специалистов не соответствуют их именам, так как были загружены в порядке появления на странице, а не по соответствию специалистам.

## 🛠️ Решение

Создано 2 инструмента для исправления:

### Вариант 1: Визуальный инструмент (Рекомендуется)

#### Шаг 1: Запустите сервер
```bash
npm start
```

#### Шаг 2: Откройте инструмент
Откройте в браузере:
```
http://localhost:8000/photo-mapping-tool.html
```

#### Шаг 3: Сопоставьте фотографии
1. Просмотрите каждую карточку специалиста
2. Если фото не соответствует имени, выберите правильное из выпадающего списка
3. Нажмите "Сгенерировать код"

#### Шаг 4: Примените изменения
1. Скопируйте сгенерированный код
2. Откройте файл `backend/fix-specialist-photos-mapping.js`
3. Найдите строку:
   ```javascript
   const CORRECT_PHOTO_MAPPING = {
       // Пример:
   };
   ```
4. Замените на скопированный код
5. Запустите:
   ```bash
   node backend/fix-specialist-photos-mapping.js
   ```

---

### Вариант 2: Ручное редактирование

#### Шаг 1: Просмотрите текущее состояние
```bash
node backend/fix-specialist-photos-mapping.js
```

Вы увидите список:
```
[1] Маргарита Румянцева       → specialist_1_маргарита_румянцева.jpg
[2] Ольга П.                  → specialist_2_ольга_п.jpg
...
```

#### Шаг 2: Просмотрите изображения
```bash
open images/specialists/
# или
ls -lh images/specialists/
```

Откройте каждое изображение и сопоставьте с именами специалистов.

#### Шаг 3: Создайте маппинг
Откройте `backend/fix-specialist-photos-mapping.js` и заполните:

```javascript
const CORRECT_PHOTO_MAPPING = {
    2: "specialist_15_анна_ж.jpg",      // Если фото Ольги П. это specialist_15
    5: "specialist_10_владимир.jpg",    // Если фото Марины это specialist_10
    10: "specialist_5_марина.jpg",      // Если фото Владимира это specialist_5
    // ... и так далее для всех несоответствий
};
```

#### Шаг 4: Примените изменения
```bash
node backend/fix-specialist-photos-mapping.js
```

---

## 📋 Текущее состояние

Запустите для просмотра:
```bash
node backend/fix-specialist-photos-mapping.js
```

## ✅ Проверка результата

### 1. Проверить в БД:
```bash
node -e "const initSqlJs = require('sql.js'); const fs = require('fs'); (async () => { const SQL = await initSqlJs(); const db = new SQL.Database(fs.readFileSync('backend/database.sqlite')); const result = db.exec('SELECT id, name, photo FROM specialists LIMIT 10'); console.log(JSON.stringify(result, null, 2)); })();"
```

### 2. Проверить на сайте:
1. Откройте: `http://localhost:8000/specialists.html`
2. Проверьте фотографии в карточках
3. Откройте профиль специалиста: `specialist-profile.html?id=1`

### 3. Проверить в админке:
1. Откройте админку
2. Перейдите в "Специалисты"
3. Проверьте фотографии

---

## 🔍 Советы по сопоставлению

### Признаки правильного фото:
- Профессиональное фото
- Соответствует полу специалиста
- Соответствует возрасту (опыт работы)
- Стиль фото соответствует профессии

### Если не уверены:
1. Проверьте оригинальный сайт: https://dr-rumyantceva.ru/specialists/
2. Найдите специалиста по имени
3. Сравните фото

---

## 📊 Пример исправления

### До:
```
[2] Ольга П. → specialist_2_ольга_п.jpg  ❌ (неправильное фото)
[5] Марина   → specialist_5_марина.jpg   ❌ (неправильное фото)
```

### Маппинг:
```javascript
const CORRECT_PHOTO_MAPPING = {
    2: "specialist_15_анна_ж.jpg",    // Правильное фото Ольги П.
    5: "specialist_10_владимир.jpg",  // Правильное фото Марины
};
```

### После:
```
[2] Ольга П. → specialist_15_анна_ж.jpg     ✅
[5] Марина   → specialist_10_владимир.jpg   ✅
```

---

## 🚨 Важно

1. **Сделайте бэкап БД** перед изменениями:
   ```bash
   cp backend/database.sqlite backend/database.sqlite.backup
   ```

2. **Проверьте все изменения** перед применением

3. **Если ошиблись**, восстановите из бэкапа:
   ```bash
   cp backend/database.sqlite.backup backend/database.sqlite
   ```

---

## 📁 Файлы

- `photo-mapping-tool.html` - Визуальный инструмент
- `backend/fix-specialist-photos-mapping.js` - Скрипт применения изменений
- `images/specialists/` - Директория с фотографиями

---

## 💡 Альтернативный подход

Если фотографий слишком много для ручного сопоставления, можно:

1. Загрузить правильные фото через админку
2. Использовать функцию загрузки в админ-панели
3. Каждый специалист может быть отредактирован индивидуально

---

## ✅ Готово!

После исправления все фотографии будут соответствовать специалистам.
