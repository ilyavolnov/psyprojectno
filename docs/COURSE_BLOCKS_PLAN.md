# План реализации блоков страницы курса

## Анализ структуры

### Текущая структура страницы курса:

1. **Hero Block** - Главный блок
   - Фото курса
   - Название
   - Цена
   - Дата старта
   - Кнопка оплаты
   - Инструкция по оплате

2. **Description Block** - Описание
   - Изображение
   - Заголовок
   - Подзаголовок
   - Тип контента

3. **Program Block** - Программа курса
   - Список тем/модулей
   - Подпункты

4. **Features Block** - Преимущества
   - Изображение (слева/справа)
   - Список преимуществ

5. **Author Block** - Автор
   - Фото
   - Имя
   - Регалии

## Типы блоков

```javascript
{
  type: 'hero',
  data: {
    image: string,
    title: string,
    price: number,
    startDate: string,
    paymentInstructions: string
  }
}

{
  type: 'description',
  data: {
    image: string,
    title: string,
    subtitle: string,
    contentType: string
  }
}

{
  type: 'program',
  data: {
    title: string,
    items: string[]
  }
}

{
  type: 'features',
  data: {
    image: string,
    imagePosition: 'left' | 'right',
    title: string,
    items: string[]
  }
}

{
  type: 'author',
  data: {
    photo: string,
    name: string,
    credentials: string[]
  }
}
```

## Реализация

### ✅ Выполнено:

1. Создана миграция БД - добавлено поле `page_blocks` в таблицу `courses`
2. Создан файл `admin/js/admin-course-blocks.js` с конструктором блоков
3. Подключен скрипт в `admin/pages/admin-panel.html`

### 🔄 Следующие шаги:

1. Обновить `admin/js/admin-courses-certificates.js`:
   - Добавить вкладки в попап курса (Основное / Блоки страницы)
   - Интегрировать конструктор блоков
   - Сохранять `page_blocks` при сохранении курса

2. Обновить `backend/routes/courses.js`:
   - Добавить поддержку поля `page_blocks` в PUT/POST запросах

3. Создать страницу курса `course-page.html`:
   - Рендеринг блоков на фронтенде
   - Стили для каждого типа блока

4. Создать `course-page-loader.js`:
   - Загрузка данных курса по ID
   - Рендеринг блоков

## CSS стили для блоков

Нужно добавить стили для:
- `.course-hero` - главный блок
- `.course-description` - описание
- `.course-program` - программа
- `.course-features` - преимущества
- `.course-author` - автор

## Пример использования

```javascript
// В админке
const blocks = [
  {
    type: 'hero',
    data: {
      image: '/images/course1.jpg',
      title: 'Марафон по РПП',
      price: 9900,
      startDate: '10 НОЯБРЯ',
      paymentInstructions: 'Высылайте скрин оплаты...'
    }
  },
  {
    type: 'program',
    data: {
      title: 'Программа курса',
      items: [
        'Почему мы переедаем?',
        'Прокрастинация через еду',
        'Переедание выходного дня'
      ]
    }
  }
];

// Сохранение
await saveCourse(courseId, { page_blocks: JSON.stringify(blocks) });

// На странице курса
const course = await loadCourse(courseId);
const blocks = JSON.parse(course.page_blocks || '[]');
blocks.forEach(block => renderBlock(block));
```

## Преимущества подхода

1. ✅ Гибкость - можно создавать любую структуру страницы
2. ✅ Переиспользование - одинаковый подход для специалистов и курсов
3. ✅ Простота - визуальный конструктор в админке
4. ✅ Масштабируемость - легко добавлять новые типы блоков
