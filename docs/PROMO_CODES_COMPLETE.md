# Система промокодов - Полная реализация

## ✅ Что реализовано

### 1. Backend API (`backend/routes/promo-codes.js`)

**Эндпоинты:**
- `GET /api/promo-codes` - получить все промокоды
- `POST /api/promo-codes/validate` - валидация промокода
- `POST /api/promo-codes` - создать промокод
- `PUT /api/promo-codes/:id` - обновить промокод
- `DELETE /api/promo-codes/:id` - удалить промокод

**Валидация включает:**
- Проверка активности промокода
- Проверка срока действия (valid_from, valid_until)
- Проверка лимита использований (max_uses)
- Автоматическое преобразование кода в верхний регистр

### 2. База данных

**Таблица `promo_codes`:**
```sql
CREATE TABLE promo_codes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code TEXT NOT NULL UNIQUE,
    discount INTEGER NOT NULL,
    description TEXT,
    max_uses INTEGER DEFAULT 0,
    used_count INTEGER DEFAULT 0,
    valid_from DATETIME,
    valid_until DATETIME,
    status TEXT DEFAULT 'active',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
)
```

**Дефолтные промокоды:**
- `WELCOME10` - 10% скидка для новых клиентов
- `SAVE20` - 20% скидка
- `FIRST` - 15% скидка на первый заказ

### 3. Админ-панель (`admin/js/admin-promo-codes.js`)

**Функционал:**
- ✅ Просмотр всех промокодов в таблице
- ✅ Создание нового промокода
- ✅ Редактирование существующего промокода
- ✅ Удаление промокода
- ✅ Отображение статистики использования
- ✅ Управление сроками действия
- ✅ Установка лимитов использования

**Интерфейс:**
- Таблица с полной информацией о промокодах
- Модальное окно для создания/редактирования
- Бейджи для статусов и скидок
- Адаптивный дизайн

### 4. Интеграция в попап заказа (`order-popup.js`)

**Функционал:**
- ✅ Кнопка "Ввести промокод" с иконкой 🎁
- ✅ Поле ввода промокода (скрыто по умолчанию)
- ✅ Кнопка "Применить" для валидации
- ✅ Реал-тайм валидация через API
- ✅ Визуальное отображение скидки (зачеркнутая старая цена)
- ✅ Сообщения об успехе/ошибке
- ✅ Блокировка поля после применения промокода
- ✅ Отправка промокода вместе с заказом

## 🎯 Как использовать

### Для администратора:

1. Войти в админ-панель: http://localhost:8000/admin-login.html
2. Перейти в раздел "Промокоды"
3. Создать новый промокод:
   - Код (например: SUMMER2024)
   - Скидка в процентах (1-100)
   - Описание (опционально)
   - Максимальное количество использований (0 = без ограничений)
   - Срок действия (опционально)
   - Статус (активен/неактивен)

### Для клиента:

1. Открыть попап заказа на любой странице
2. Нажать "🎁 Ввести промокод"
3. Ввести код промокода
4. Нажать "Применить"
5. Увидеть новую цену со скидкой
6. Оформить заказ

## 📊 Примеры использования API

### Получить все промокоды:
```bash
curl http://localhost:3001/api/promo-codes
```

### Валидировать промокод:
```bash
curl -X POST http://localhost:3001/api/promo-codes/validate \
  -H "Content-Type: application/json" \
  -d '{"code":"WELCOME10"}'
```

### Создать промокод:
```bash
curl -X POST http://localhost:3001/api/promo-codes \
  -H "Content-Type: application/json" \
  -d '{
    "code": "NEWYEAR",
    "discount": 25,
    "description": "Новогодняя скидка",
    "max_uses": 100,
    "valid_until": "2024-01-31"
  }'
```

## 🔧 Технические детали

### Валидация промокода:
1. Проверка существования кода в БД
2. Проверка статуса (должен быть 'active')
3. Проверка даты начала действия
4. Проверка даты окончания действия
5. Проверка лимита использований
6. Возврат процента скидки

### Применение скидки:
1. Получение оригинальной цены
2. Расчет новой цены: `price * (1 - discount/100)`
3. Округление до целого числа
4. Визуальное отображение обеих цен
5. Сохранение промокода для отправки с заказом

### Безопасность:
- Все коды автоматически преобразуются в верхний регистр
- Валидация на стороне сервера
- Проверка всех условий перед применением
- Защита от повторного использования (через used_count)

## 📁 Файлы системы

```
backend/
├── routes/promo-codes.js          # API маршруты
├── migrate-add-promo-codes.js     # Миграция БД

admin/
└── js/admin-promo-codes.js        # Админ-панель

order-popup.js                      # Интеграция в попап
styles.css                          # Стили попапа
admin/css/admin-panel-styles.css   # Стили админки
```

## ✨ Особенности

1. **Гибкая настройка**: можно установить любой процент скидки, лимиты, сроки
2. **Статистика**: отслеживание количества использований
3. **Удобный интерфейс**: простое управление через админ-панель
4. **Реал-тайм валидация**: мгновенная проверка промокода
5. **Визуальная обратная связь**: понятные сообщения об успехе/ошибке
6. **Адаптивность**: работает на всех устройствах

## 🚀 Готово к использованию!

Система промокодов полностью функциональна и готова к использованию в продакшене.
