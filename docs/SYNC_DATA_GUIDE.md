# Руководство по синхронизации данных

## 🔄 Проблема

В проекте используются два источника данных:

1. **База данных** (`backend/database.sqlite`) - используется админкой и API
2. **JSON файл** (`specialists-data.json`) - используется страницей специалистов

Это может привести к рассинхронизации данных.

## ✅ Решение

### Автоматическая синхронизация

Запустите скрипт синхронизации:

```bash
node backend/sync-specialists-json.js
```

Этот скрипт:
- Читает данные из БД
- Обновляет `specialists-data.json`
- Синхронизирует фотографии, имена, цены, статусы и т.д.

### Когда запускать синхронизацию?

Запускайте после:
- Изменения данных в админке
- Обновления фотографий
- Изменения цен или статусов
- Добавления/удаления специалистов

## 🔧 Улучшенная загрузка данных

Страница `specialists.html` теперь:

1. **Сначала пытается загрузить из API** (всегда актуальные данные)
2. **Если API недоступен** → загружает из JSON файла (fallback)

Это означает:
- ✅ Если сервер запущен → данные всегда актуальные
- ✅ Если сервер не запущен → работает из JSON

## 📋 Источники данных по страницам

| Страница | Источник данных | Примечание |
|----------|----------------|------------|
| `specialists.html` | API → JSON | Приоритет API |
| `specialist-profile.html` | API → JSON | Приоритет API |
| Админка | БД (через API) | Только БД |

## 🚀 Автоматизация

### Вариант 1: Синхронизация при сохранении в админке

Добавьте в `admin/js/admin-panel.js` после сохранения специалиста:

```javascript
// После успешного сохранения
await fetch('http://localhost:3001/api/sync-json', { method: 'POST' });
```

### Вариант 2: Периодическая синхронизация

Добавьте в `package.json`:

```json
{
  "scripts": {
    "sync": "node backend/sync-specialists-json.js",
    "start": "npm run sync && npm run start:all"
  }
}
```

Теперь при каждом запуске проекта данные будут синхронизироваться.

## 🔍 Проверка синхронизации

### Проверить данные в БД:
```bash
node -e "const initSqlJs = require('sql.js'); const fs = require('fs'); (async () => { const SQL = await initSqlJs(); const db = new SQL.Database(fs.readFileSync('backend/database.sqlite')); const result = db.exec('SELECT id, name, photo FROM specialists LIMIT 5'); console.log(JSON.stringify(result, null, 2)); })();"
```

### Проверить данные в JSON:
```bash
cat specialists-data.json | grep -A 3 '"id": 1'
```

### Сравнить фотографии:
```bash
# Из БД
node -e "const initSqlJs = require('sql.js'); const fs = require('fs'); (async () => { const SQL = await initSqlJs(); const db = new SQL.Database(fs.readFileSync('backend/database.sqlite')); const result = db.exec('SELECT id, name, photo FROM specialists WHERE id=1'); console.log(result[0].values[0]); })();"

# Из JSON
cat specialists-data.json | grep -A 5 '"id": 1' | grep photo
```

## 💡 Рекомендации

### Для разработки:
1. Всегда запускайте сервер: `npm start`
2. Данные будут загружаться из API (актуальные)
3. Синхронизация JSON не обязательна

### Для продакшена:
1. Синхронизируйте JSON перед деплоем
2. Настройте автоматическую синхронизацию
3. Или используйте только API (удалите fallback на JSON)

## 🎯 Миграция на полностью API-based подход

Если хотите использовать только API:

### 1. Удалите fallback в `specialists-loader.js`:

```javascript
async loadData() {
    try {
        const response = await fetch('http://localhost:3001/api/specialists');
        const data = await response.json();
        
        if (data.success) {
            this.specialists = data.data;
            // Sort...
        }
    } catch (error) {
        console.error('Error loading specialists:', error);
    }
}
```

### 2. Обновите URL API для продакшена:

```javascript
const API_URL = process.env.API_URL || 'http://localhost:3001';
const response = await fetch(`${API_URL}/api/specialists`);
```

### 3. Удалите `specialists-data.json` (опционально)

## ✅ Итог

Теперь данные синхронизированы! 

- Админка всегда работает с БД
- Страницы загружают из API (если доступен)
- JSON файл - резервный источник
- Скрипт синхронизации обновляет JSON из БД

Запускайте `node backend/sync-specialists-json.js` после изменений в админке.
