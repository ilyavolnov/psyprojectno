# Деплой проекта на Vercel

## 📋 Подготовка

### 1. Установите Vercel CLI (если еще не установлен)
```bash
npm install -g vercel
```

### 2. Войдите в аккаунт Vercel
```bash
vercel login
```

## 🚀 Деплой

### Вариант 1: Через CLI (рекомендуется)

1. **Первый деплой (тестовый)**
```bash
vercel
```

Vercel задаст несколько вопросов:
- Set up and deploy? → Yes
- Which scope? → Выберите ваш аккаунт
- Link to existing project? → No
- What's your project's name? → psyproject (или свое название)
- In which directory is your code located? → ./
- Want to override the settings? → No

2. **Продакшн деплой**
```bash
vercel --prod
```

### Вариант 2: Через GitHub (автоматический деплой)

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "New Project"
3. Импортируйте ваш GitHub репозиторий
4. Vercel автоматически определит настройки
5. Нажмите "Deploy"

## ⚙️ Настройка переменных окружения

После деплоя нужно добавить переменные окружения:

1. Зайдите в настройки проекта на Vercel
2. Перейдите в Settings → Environment Variables
3. Добавьте следующие переменные:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
ADMIN_CHAT_ID=ваш_chat_id
NODE_ENV=production
```

## 📁 Структура проекта для Vercel

```
psyproject/
├── api/
│   └── index.js          # Serverless функция для API
├── backend/
│   ├── routes/           # API маршруты
│   ├── database.js       # База данных
│   └── server.js         # Express сервер
├── *.html                # Frontend файлы
├── *.css                 # Стили
├── *.js                  # Frontend скрипты
├── vercel.json           # Конфигурация Vercel
└── package.json          # Зависимости
```

## ⚠️ Важные замечания

### База данных
Vercel использует serverless функции, которые не сохраняют состояние между запросами. Для продакшена рекомендуется:

1. **Использовать внешнюю БД** (рекомендуется):
   - PostgreSQL (Vercel Postgres, Supabase, Neon)
   - MongoDB (MongoDB Atlas)
   - MySQL (PlanetScale)

2. **Или использовать Vercel KV** для простых данных:
   ```bash
   npm install @vercel/kv
   ```

### Telegram Bot
На Vercel бот не может работать в режиме long polling. Нужно использовать webhooks:

1. Создайте отдельный endpoint для webhook:
```javascript
// api/webhook.js
module.exports = async (req, res) => {
    // Обработка webhook от Telegram
};
```

2. Настройте webhook в Telegram:
```bash
curl -X POST https://api.telegram.org/bot<TOKEN>/setWebhook \
  -d url=https://your-domain.vercel.app/api/webhook
```

### Файлы загрузки
Vercel имеет ограничение на размер файлов (50MB). Для загрузки файлов используйте:
- Vercel Blob Storage
- AWS S3
- Cloudinary

## 🔧 Обновление после изменений

```bash
# Коммит изменений
git add .
git commit -m "Update"
git push

# Если используете CLI
vercel --prod
```

При использовании GitHub интеграции деплой происходит автоматически при push.

## 🌐 URL проекта

После деплоя вы получите URL вида:
- Preview: `https://psyproject-xxx.vercel.app`
- Production: `https://psyproject.vercel.app`

Можно также подключить свой домен в настройках проекта.

## 📊 Мониторинг

1. Логи доступны в Vercel Dashboard → Deployments → Logs
2. Аналитика: Dashboard → Analytics
3. Ошибки: Dashboard → Errors

## 🐛 Решение проблем

### Ошибка "Module not found"
```bash
# Убедитесь что все зависимости установлены
npm install
vercel --prod
```

### База данных не работает
- Проверьте путь к database.db
- Рассмотрите миграцию на внешнюю БД

### API не отвечает
- Проверьте логи в Vercel Dashboard
- Убедитесь что переменные окружения настроены
- Проверьте пути в vercel.json

## 📝 Чеклист перед деплоем

- [ ] Все изменения закоммичены
- [ ] .gitignore настроен (node_modules, .env)
- [ ] package.json содержит все зависимости
- [ ] vercel.json настроен
- [ ] Переменные окружения подготовлены
- [ ] База данных готова (или план миграции)
- [ ] Telegram bot настроен на webhooks (если нужен)

## 🎯 Следующие шаги

1. Деплой на Vercel
2. Настройка переменных окружения
3. Миграция БД на внешний сервис (если нужно)
4. Настройка webhook для Telegram бота
5. Подключение своего домена (опционально)
6. Настройка мониторинга и алертов

## 📚 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel CLI](https://vercel.com/docs/cli)
- [Serverless Functions](https://vercel.com/docs/functions)
- [Environment Variables](https://vercel.com/docs/environment-variables)
