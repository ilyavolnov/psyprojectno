# Интеграция супервизий в админку

## Что реализовано

Добавлена полная система управления супервизиями через админ-панель с интеграцией на фронтенд.

## Backend

### 1. База данных
**Таблица:** `supervisions`
```sql
- id (PRIMARY KEY)
- name (имя супервизора)
- title (название супервизии)
- image (URL изображения)
- price (цена)
- duration (длительность)
- experience (опыт)
- tags (JSON массив тегов)
- description (JSON массив описаний)
- education (JSON массив образования)
- status (available/unavailable)
- created_at, updated_at
```

### 2. API маршруты
**Файл:** `backend/routes/supervisions.js`

- `GET /api/supervisions` - получить все супервизии
- `GET /api/supervisions/:id` - получить одну супервизию
- `POST /api/supervisions` - создать супервизию
- `PUT /api/supervisions/:id` - обновить супервизию
- `DELETE /api/supervisions/:id` - удалить супервизию

### 3. Миграции
- `backend/migrate-add-supervisions.js` - создание таблицы
- `backend/migrate-supervisions-from-json.js` - миграция из JSON

## Admin Panel

### 1. Навигация
Добавлена вкладка **🎓 Супервизии** в боковом меню

### 2. Управление супервизиями
**Файл:** `admin/js/admin-supervisions.js`

**Функции:**
- `loadSupervisions()` - загрузка и отображение списка
- `openSupervisionPopup(id)` - открытие формы создания/редактирования
- `saveSupervision(id)` - сохранение супервизии
- `deleteSupervision(id)` - удаление супервизии

**Поля формы:**
- Имя супервизора *
- Название супервизии *
- Цена (₽)
- Длительность
- Опыт
- Изображение (URL)
- Теги (по одному на строку)
- Описание (по одному абзацу на строку)
- Образование (по одному пункту на строку)
- Статус (Доступна/Недоступна)

### 3. Таблица супервизий
Отображает:
- ID
- Имя
- Название
- Цена
- Длительность
- Опыт
- Статус
- Действия (Редактировать/Удалить)

## Frontend

### 1. Загрузчик данных
**Файл:** `supervisions-loader.js`

Автоматически загружает супервизии из API и отображает на странице `supervision.html`

**Что отображается на карточке:**
- Изображение супервизора
- Имя
- Название супервизии
- Теги
- Длительность и опыт
- Цена
- Кнопка "Подробнее" (открывает popup)

**Popup с деталями:**
При клике на "Подробнее" открывается popup с полной информацией:
- Фото супервизора
- Имя и название
- Теги
- Длительность, опыт, стоимость
- Полное описание супервизии
- Образование и обучение
- Кнопка "Записаться на супервизию"

### 2. Интеграция
Подключен к `supervision.html`:
```html
<script src="supervisions-loader.js"></script>
```

## Использование

### Добавление супервизии через админку

1. Откройте админ-панель
2. Перейдите в раздел "Супервизии"
3. Нажмите "➕ Добавить супервизию"
4. Заполните форму:
   - Имя супервизора (обязательно)
   - Название супервизии (обязательно)
   - Остальные поля по желанию
5. Нажмите "💾 Создать"
6. Супервизия появится на сайте автоматически

### Редактирование супервизии

1. В таблице супервизий нажмите "Редактировать"
2. Измените нужные поля
3. Нажмите "💾 Сохранить"
4. Изменения применятся на сайте

### Удаление супервизии

1. В таблице супервизий нажмите "Удалить"
2. Подтвердите удаление
3. Супервизия исчезнет с сайта

## Формат данных

### Теги
Вводятся по одному на строку:
```
Практикующий психолог
EMDR-терапевт
Сертифицированный супервизор
```

### Описание
Вводится по одному абзацу на строку (разделитель - двойной перенос):
```
Разбор кейса с получением практических рекомендаций.

Супервизии проходят в интегративном подходе.
```

### Образование
Вводится по одному пункту на строку:
```
Уди Орен
Джоани Спирингс
Долорас Маскуэра
```

## Миграция существующих данных

Если есть данные в `supervisions-data.json`:
```bash
node backend/migrate-supervisions-from-json.js
```

Это перенесет все супервизии из JSON в базу данных.

## Преимущества

✅ Управление через админку - не нужно редактировать JSON
✅ Автоматическое обновление на сайте
✅ Валидация данных
✅ История изменений (created_at, updated_at)
✅ Статусы (можно скрыть супервизию)
✅ Единый источник данных (база данных)

## Статус

✅ Backend API - готово
✅ База данных - готова
✅ Админ-панель - готова
✅ Frontend загрузчик - готов
✅ Интеграция - готова

Система полностью функциональна и готова к использованию!
