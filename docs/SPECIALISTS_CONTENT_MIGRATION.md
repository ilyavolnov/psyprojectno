## Миграция контента специалистов в БД

### ✅ Выполнено

#### Этап 1: Подготовка данных
- ✅ Скачаны изображения всех 42 специалистов
- ✅ Обновлены пути к изображениям в БД
- ✅ Создан MD файл с контентом из `specialists-data.json`

#### Этап 2: Загрузка в БД
- ✅ Обновлено 42 специалиста
- ✅ Создано 91 блок контента
- ✅ Добавлено 3 отзыва
- ✅ Заполнены поля: description, education, specialization, additional_services

### 📊 Статистика

```
Специалистов обработано: 42
Блоков создано: 91
Отзывов добавлено: 3
Ошибок: 0
```

### 🗂️ Типы созданных блоков

#### 1. **about** - О специалисте
- Содержит роль и дополнительные услуги
- Используется для основного описания

#### 2. **list** - Списки
- Методы терапии
- Образование и квалификация
- Специализации

#### 3. **testimonials** - Отзывы
- Текст отзыва
- Автор отзыва

### 📝 Структура данных в БД

#### Таблица `specialists`

```sql
CREATE TABLE specialists (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    photo TEXT,
    specialization TEXT,        -- Заполнено ✅
    experience INTEGER,
    price INTEGER,
    status TEXT,
    description TEXT,           -- Заполнено ✅
    education TEXT,             -- Заполнено ✅
    additional_services TEXT,   -- Заполнено ✅
    testimonials TEXT,          -- JSON, Заполнено ✅
    page_blocks TEXT,           -- JSON, Заполнено ✅
    created_at DATETIME,
    updated_at DATETIME
);
```

#### Формат `page_blocks` (JSON)

```json
[
  {
    "type": "about",
    "title": "О специалисте",
    "content": "Врач-психотерапевт, EMDR-терапевт..."
  },
  {
    "type": "list",
    "title": "Методы терапии",
    "items": [
      "Когнитивно-поведенческая психотерапия",
      "EMDR-терапия",
      "..."
    ]
  },
  {
    "type": "testimonials",
    "title": "Отзывы клиентов",
    "testimonials": [
      {
        "text": "Отличный специалист!",
        "author": "Анна"
      }
    ]
  }
]
```

#### Формат `testimonials` (JSON)

```json
[
  {
    "text": "Текст отзыва",
    "author": "Имя автора"
  }
]
```

### 🎯 Как это работает

#### 1. Рендеринг страницы специалиста

Файл: `specialist-profile-loader.js`

```javascript
// Загрузка данных
const specialist = await loadSpecialist(id);

// Парсинг блоков
if (typeof specialist.page_blocks === 'string') {
    specialist.page_blocks = JSON.parse(specialist.page_blocks);
}

// Рендеринг блоков
specialist.page_blocks.forEach(block => {
    renderBlock(block);
});
```

#### 2. Редактирование в админке

Файл: `admin/js/admin-specialist-blocks.js`

```javascript
// Инициализация блоков
window.initBlocksSystem(specialist.page_blocks);

// Добавление нового блока
window.addBlock('list');

// Сохранение
const blocks = window.getBlocksData();
await updateSpecialist(id, { page_blocks: JSON.stringify(blocks) });
```

### 💡 Рекомендации

#### ✅ Существующие типы блоков (достаточно)

1. **text** - Текстовый блок
2. **list** - Список пунктов
3. **about** - О специалисте
4. **payment** - Схема оплаты
5. **testimonials** - Отзывы

#### 🆕 Рекомендуемые дополнительные типы блоков

##### 1. **quote** - Цитата/Важная информация
```javascript
{
    type: 'quote',
    title: 'Важно',
    content: 'Текст цитаты',
    author: 'Автор (опционально)'
}
```

**Применение:** Выделение ключевых мыслей, философии работы

##### 2. **accordion** - Раскрывающиеся блоки (FAQ)
```javascript
{
    type: 'accordion',
    title: 'Часто задаваемые вопросы',
    items: [
        {
            question: 'Вопрос 1',
            answer: 'Ответ 1'
        }
    ]
}
```

**Применение:** FAQ, подробная информация о методах

##### 3. **timeline** - Временная шкала
```javascript
{
    type: 'timeline',
    title: 'Опыт работы',
    items: [
        {
            year: '2020-2023',
            title: 'Должность',
            description: 'Описание'
        }
    ]
}
```

**Применение:** Карьерный путь, этапы обучения

##### 4. **gallery** - Галерея изображений
```javascript
{
    type: 'gallery',
    title: 'Сертификаты',
    images: [
        {
            url: '/images/cert1.jpg',
            caption: 'Описание'
        }
    ]
}
```

**Применение:** Сертификаты, дипломы, фото с мероприятий

##### 5. **video** - Видео блок
```javascript
{
    type: 'video',
    title: 'Видео-презентация',
    url: 'https://youtube.com/...',
    description: 'Описание видео'
}
```

**Применение:** Видео-визитка, записи выступлений

##### 6. **stats** - Статистика/Достижения
```javascript
{
    type: 'stats',
    title: 'Достижения',
    items: [
        {
            number: '500+',
            label: 'Клиентов'
        },
        {
            number: '15',
            label: 'Лет опыта'
        }
    ]
}
```

**Применение:** Ключевые цифры, достижения

### 🔧 Как добавить новый тип блока

#### 1. Обновить конфигурацию в `admin-specialist-blocks.js`

```javascript
const BLOCK_TYPES = {
    // ... существующие типы
    quote: {
        name: 'Цитата',
        icon: '💭',
        fields: ['title', 'content', 'author']
    }
};
```

#### 2. Добавить генерацию полей

```javascript
function generateBlockFields(block, index) {
    // ...
    case 'quote':
        return `
            <div class="admin-form-group">
                <label>Текст цитаты</label>
                <textarea class="admin-form-input block-field" 
                          data-block-index="${index}" 
                          data-field="content">${block.content || ''}</textarea>
            </div>
            <div class="admin-form-group">
                <label>Автор (опционально)</label>
                <input type="text" class="admin-form-input block-field" 
                       data-block-index="${index}" 
                       data-field="author" 
                       value="${block.author || ''}">
            </div>
        `;
}
```

#### 3. Добавить рендеринг в `specialist-profile-loader.js`

```javascript
renderBlock(block) {
    // ...
    case 'quote':
        return `
            <div class="profile-block quote-block">
                <blockquote class="quote-content">
                    ${block.content}
                    ${block.author ? `<cite>— ${block.author}</cite>` : ''}
                </blockquote>
            </div>
        `;
}
```

#### 4. Добавить стили в `styles.css`

```css
.quote-block {
    background: #f8f9fa;
    border-left: 4px solid var(--color-blue);
    padding: 30px;
    margin: 30px 0;
}

.quote-content {
    font-size: 20px;
    font-style: italic;
    line-height: 1.6;
}

.quote-content cite {
    display: block;
    margin-top: 15px;
    font-size: 16px;
    font-style: normal;
    color: #666;
}
```

### 📁 Использованные скрипты

1. **scrape-specialist-images.js** - Загрузка изображений
2. **update-specialist-photos.js** - Обновление путей в JSON
3. **migrate-update-specialist-photos.js** - Обновление путей в БД
4. **prepare-specialists-data.js** - Подготовка MD файла
5. **migrate-specialists-content-to-db.js** - Загрузка контента в БД

### ✅ Проверка результата

#### Проверить данные в БД:
```bash
node -e "const initSqlJs = require('sql.js'); const fs = require('fs'); (async () => { const SQL = await initSqlJs(); const db = new SQL.Database(fs.readFileSync('backend/database.sqlite')); const result = db.exec('SELECT id, name, LENGTH(page_blocks) as blocks_size FROM specialists LIMIT 5'); console.log(JSON.stringify(result, null, 2)); })();"
```

#### Проверить на сайте:
1. Откройте страницу специалиста: `specialist-profile.html?id=1`
2. Проверьте отображение блоков
3. Откройте админку → Специалисты → Редактировать
4. Проверьте блоки в редакторе

### 🎉 Итог

Все данные специалистов успешно загружены в базу данных:
- ✅ 42 специалиста обновлены
- ✅ 91 блок контента создан
- ✅ 3 отзыва добавлены
- ✅ Все поля заполнены
- ✅ Система блоков работает
- ✅ Админка готова к редактированию

Система готова к использованию! При необходимости можно добавить новые типы блоков по рекомендациям выше.
