# Курс "Марафон по РПП" добавлен!

## ✅ Что сделано:

### 1. Создан курс в базе данных
- **ID**: 4
- **Название**: Марафон по РПП «Я больше не переедаю»
- **Цена**: 9900 ₽
- **Статус**: available (доступен)
- **Дата старта**: 10 НОЯБРЯ
- **Изображение**: images/courses/rpp/mainimaje.jpeg

### 2. Добавлены блоки страницы курса

#### Блок 1: Hero (Главный блок)
- Изображение курса
- Название
- Цена: 9900 ₽
- Дата старта: 10 НОЯБРЯ
- Инструкция по оплате (WhatsApp)

#### Блок 2: Description (Описание)
- Заголовок: "Почему мы переедаем?"
- Описание проекта
- Тип контента: "Лекция + презентация"

#### Блок 3: Program (Программа курса)
27 пунктов программы:
- Прокрастинация через еду
- Переедание выходного дня
- Чек-лист на неблагополучное ПП
- Почему диеты не работают?
- EMDR-сессии
- IFS-сессия
- Медитации из ДПТ
- Практикум «Забота о теле»
- И многое другое...

#### Блок 4: Features (Преимущества)
- Индивидуальное сопровождение психологом
- Удобный доступ к материалам
- Информация без воды
- 3 недели изменений

#### Блок 5: Author (Автор)
- Фото: Маргарита Румянцева
- 9 регалий и достижений

### 3. Обновлена главная страница
- Курс РПП перемещен на **первое место** в списке курсов
- Обновлена цена: 9900 ₽
- Обновлен статус: "В доступе"
- Добавлено изображение: images/courses/rpp/mainimaje.jpeg
- Добавлен атрибут `data-course-id="4"` для связи с БД

## 📋 Следующие шаги:

### 1. Создать страницу курса
Создать `course-page.html` с рендерингом блоков:
```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <title>Марафон по РПП</title>
</head>
<body>
    <div id="course-content"></div>
    <script src="course-page-loader.js"></script>
</body>
</html>
```

### 2. Создать загрузчик
`course-page-loader.js`:
```javascript
async function loadCourse() {
    const urlParams = new URLSearchParams(window.location.search);
    const courseId = urlParams.get('id');
    
    const response = await fetch(`http://localhost:3001/api/courses/${courseId}`);
    const data = await response.json();
    
    if (data.success) {
        const course = data.data;
        const blocks = JSON.parse(course.page_blocks || '[]');
        renderBlocks(blocks);
    }
}
```

### 3. Добавить ссылку на курс
На главной странице добавить обработчик клика:
```javascript
document.querySelectorAll('[data-course-id]').forEach(item => {
    item.addEventListener('click', () => {
        const courseId = item.dataset.courseId;
        window.location.href = `course-page.html?id=${courseId}`;
    });
});
```

## 🎯 Как проверить:

### В админке:
1. Открыть http://localhost:3001/admin/pages/admin-panel.html
2. Перейти в "Курсы"
3. Найти "Марафон по РПП"
4. Нажать "Редактировать"
5. Переключиться на вкладку "Блоки страницы"
6. Увидеть 5 блоков с данными

### На главной странице:
1. Открыть index.html
2. Прокрутить до секции "Курсы"
3. Увидеть "Марафон по РПП" на первом месте
4. Цена: 9900 ₽
5. Статус: "В доступе"

### Через API:
```bash
curl http://localhost:3001/api/courses/4
```

## 📁 Созданные файлы:
- `backend/add-rpp-course.js` - скрипт добавления курса
- `RPP_COURSE_ADDED.md` - эта документация

## 📝 Измененные файлы:
- `index.html` - обновлена секция курсов

## 💡 Структура данных курса:

```json
{
  "id": 4,
  "title": "Марафон по РПП «Я больше не переедаю»",
  "price": 9900,
  "status": "available",
  "image": "images/courses/rpp/mainimaje.jpeg",
  "page_blocks": "[...5 блоков...]"
}
```

## 🎨 Изображения:

Нужно добавить изображения в папку `images/courses/rpp/`:
- `mainimaje.jpeg` - главное изображение курса ✅
- `description.jpg` - изображение для блока описания (опционально)
- `features.jpg` - изображение для блока преимуществ (опционально)

Если изображений нет, блоки будут работать без них.
