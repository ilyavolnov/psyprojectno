# 🔧 Быстрое исправление "Ошибка загрузки супервизий"

## Проблема
Админ-панель показывает "Ошибка загрузки супервизий" потому что:
1. База данных не содержит таблицу supervisions
2. Сервер не перезапущен после миграции

## ✅ Решение (2 команды)

### 1. Запустите миграцию:
```bash
node backend/migrate-update-supervisions.js
```

### 2. Перезапустите серверы:
```bash
./start.sh
```

**ИЛИ** используйте скрипт перезапуска backend:
```bash
./restart-backend.sh
```

### 3. Обновите админ-панель:
- Откройте http://localhost:8000/admin-login.html
- Нажмите `Cmd + Shift + R` (Mac) или `Ctrl + Shift + R` (Windows)
- Перейдите в раздел "Супервизии"

## 🎯 Проверка

Проверьте, что API работает:
```bash
curl http://localhost:3001/api/supervisions
```

Должен вернуть JSON с 5 супервизиями.

## 📝 Что было сделано

Миграция создала таблицу `supervisions` с полями:
- title, supervisors, date, experience
- price, duration, price_note
- description, features (JSON), bonus
- status, created_at, updated_at

И добавила 5 супервизий:
1. Супервизия в EMDR-подходе с Натальей С.
2. Групповая супервизия
3. Супервизия с Маргаритой Румянцевой
4. Супервизия с Натальей
5. Супервизия с Павлом

---

**Если проблема осталась:**
1. Проверьте логи сервера
2. Убедитесь что порт 3001 свободен
3. Проверьте файл `backend/database.sqlite` (должен быть не пустой)
